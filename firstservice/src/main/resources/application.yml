spring:
  application:
    name: first-service-app
server:
  port: 10100

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://discovery_server:8761/eureka}