server:
  port: 10101

spring:
  application:
    name: secondservice

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka
    refresh:
      enable: true
    register-with-eureka: true